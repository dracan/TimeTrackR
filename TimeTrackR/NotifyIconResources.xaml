<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:tb="http://www.hardcodet.net/taskbar">

    <!-- Globally declared notify icon -->
    <tb:TaskbarIcon x:Key="SysTrayNotifyIcon" IconSource="/Resources/SystemTray.ico" ToolTipText="TimeTrackR">
        <tb:TaskbarIcon.ContextMenu>
            <ContextMenu>
                <MenuItem Header="Exit" />
                <MenuItem Header="" />
            </ContextMenu>
        </tb:TaskbarIcon.ContextMenu>
        <tb:TaskbarIcon.TrayToolTip>
            <Border
              Background="White"
              BorderBrush="Orange"
              BorderThickness="2"
              CornerRadius="4"
              Opacity="1.0"
              Width="160"
              Height="40">
                <TextBlock Text="Placeholder tooltip" HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
        </tb:TaskbarIcon.TrayToolTip>
    </tb:TaskbarIcon>

</ResourceDictionary>